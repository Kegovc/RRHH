import { ActiveNavItemDirective } from './active-nav-item.directive';

describe('ActiveNavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveNavItemDirective();
    expect(directive).toBeTruthy();
  });
});
