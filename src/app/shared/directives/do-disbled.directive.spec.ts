import { DoDisbledDirective } from './do-disbled.directive';

describe('DoDisbledDirective', () => {
  it('should create an instance', () => {
    const directive = new DoDisbledDirective();
    expect(directive).toBeTruthy();
  });
});
